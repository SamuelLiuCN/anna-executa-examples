{
  "name": "anna-app-web-search-demo",
  "private": true,
  "version": "0.1.0",
  "description": "Anna App that demonstrates host-managed web search & fetch over both channels: the iframe HOST API (anna.web.*) and the Reverse RPC twin (web/search + web/fetch from a bundled Executa).",
  "scripts": {
    "dev": "anna-app dev",
    "dev:mock": "anna-app dev --mock-llm fixtures/happy-path.jsonl",
    "dev:off": "anna-app dev --no-llm",
    "validate": "anna-app validate"
  },
  "devDependencies": {
    "@anna-ai/cli": "^0.1.22"
  }
}
